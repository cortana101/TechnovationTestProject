<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.technovationtestproject">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.TechnovationTestProject">
    <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:theme="@style/Theme.TechnovationTestProject.NoActionBar">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

  <!--Ability to access the camera -->
  <uses-permission android:name="android.permission.CAMERA" />
  <!--Ability to save pictures -->
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

  <!--App cannot work without camera-->
  <uses-feature android:name="android.hardware.camera" />

</manifest>